---
description: Rules for Next.js applications
globs: ["apps/docs/**"]
---

# Next.js Rules

## Architecture
- Use **App Router** (`app/` directory).
- Server Components by default. Use `"use client"` only when necessary.

## Documentation (Fumadocs)
- This app uses `fumadocs`.
- Content is in MDX.
- Follow rules for MDX content structure.

## Styling
- Tailwind CSS.
- `fumadocs-ui` components.

## Types
- Strict TypeScript usage.
- Generate types for content.
