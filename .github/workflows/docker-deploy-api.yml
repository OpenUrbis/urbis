name: Build and Deploy - Docker API 

on:
  release:
    types: [published]
  push:
    branches: [master]

jobs:
  build_and_push:
    name: Build and Push - Web
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9
          run_install: false

      - name: Set Up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install

      - name: Build with Turborepo
        run: pnpm turbo run build --filter=@open-urbis/map-api

      - name: Turborepo Cache
        uses: actions/cache@v4
        with:
          path: |
            .turbo/cache
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Set Environment Variables
        run: |
          echo "APP_VERSION=${GITHUB_REF_NAME##*/}" >> $GITHUB_ENV
          if [[ "${{ github.event_name }}" == "push" || "${{ github.ref_name }}" =~ "staging" ]]; then
            echo "ENVIRONMENT=staging" >> $GITHUB_ENV
          else
            echo "ENVIRONMENT=production" >> $GITHUB_ENV
          fi

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set Up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker Layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./infra/docker/Dockerfile
          push: true
          build-args: |
            APP_VERSION=${{ env.APP_VERSION }}
          tags: |
            openurbis/urbis-map:${{ startsWith(github.ref_name, 'staging/') && 'staging' || 'latest' }}

  # restart_deployment:
  #   name: Restart Deployment - Web
  #   runs-on: ubuntu-latest
  #   environment: ${{ startsWith(github.ref_name, 'staging/') && 'staging' || 'production' }}
  #   needs: build_and_push
  #   permissions:
  #     packages: write
  #     contents: read
  #     attestations: write
  #     id-token: write
  #   steps:
  #     - name: Log in to Azure
  #       uses: azure/login@v2
  #       with:
  #         client-id: ${{ secrets.AZURE_CLIENT_ID }}
  #         tenant-id: ${{ secrets.AZURE_TENANT_ID }}
  #         subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

  #     - name: Set Up kubectl
  #       uses: azure/setup-kubectl@v3

  #     - name: Set Up kubelogin
  #       uses: azure/use-kubelogin@v1
  #       with:
  #         kubelogin-version: 'v0.0.26'

  #     - name: Set AKS Context
  #       id: set-context
  #       uses: azure/aks-set-context@v3
  #       with:
  #         resource-group: ${{ secrets.AZURE_RESOURCE_GROUP }}
  #         cluster-name: ${{ secrets.AZURE_CLUSTER_NAME }}
  #         admin: 'false'
  #         use-kubelogin: 'true'

  #     - name: Restart Kubernetes Deployment
  #       env:
  #         DEPLOYMENT_NAME_PROD: ${{ secrets.K8S_DEPLOYMENT_NAME_PROD || 'urbis-map-api' }}
  #         DEPLOYMENT_NAME_STAGING: ${{ secrets.K8S_DEPLOYMENT_NAME_STAGING || 'urbis-map-api' }}
  #       run: |
  #         if [ "${{ env.ENVIRONMENT }}" = "staging" ]; then
  #           kubectl rollout restart deployment ${DEPLOYMENT_NAME_STAGING:-urbis-map-api} -n urbis-map
  #         else
  #           kubectl rollout restart deployment ${DEPLOYMENT_NAME_PROD:-urbis-map-api} -n urbis-map
  #         fi
