import { PermissionScopePipe } from './permission-scope-pipe';

describe('PermissionScopePipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionScopePipe();
    expect(pipe).toBeTruthy();
  });
});
