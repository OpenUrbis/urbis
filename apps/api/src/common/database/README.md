# Configuration Files Documentation

In this folder, we have 2 different configuration files for the database connection:

1. `typeorm-config.service.ts` - This configuration is used by application
2. `cli.data-source.ts` - This configuration is used by command line data source
