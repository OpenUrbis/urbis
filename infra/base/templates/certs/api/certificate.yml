apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: urbis-map-certificate
  namespace: urbis-map
spec:
  secretName: urbis-map-certificate-tls
  dnsNames:
  - {{ .Values.domainName }}
  issuerRef:
    name: urbis-issuer
    kind: Issuer