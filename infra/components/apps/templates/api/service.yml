apiVersion: v1
kind: Service
metadata:
  name: urbis-map-api-service
  namespace: urbis-map
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: urbis-map-api